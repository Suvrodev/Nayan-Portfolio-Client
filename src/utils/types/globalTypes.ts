export type TServcie = {
  _id: number;
  title: string;
  image: string;
  popupDesc: string;
  desc: string;
};

export type TTestimonial = {
  id: string;
  image: string;
  desc: string;
  name: string;
  position: string;
  rating: number;
};
export type TFun = {
  number: number;
  title: string;
  botText: string;
};
