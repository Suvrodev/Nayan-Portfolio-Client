export type TServcie = {
  id: number;
  image?: string;
  title: string;
  text: string;
};

export type TTestimonial = {
  id: string;
  image: string;
  desc: string;
  name: string;
  position: string;
  rating: number;
};
